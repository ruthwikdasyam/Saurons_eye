# RealSense capture (direct librealsense; ROS path uses ros-humble-realsense2-camera)
pyrealsense2>=2.55.1

# SLAM / fusion / point clouds
open3d>=0.18.0

# Object detection (pulls in torch)
ultralytics>=8.3.0

# General CV + ArUco markers
opencv-contrib-python>=4.10.0

# Numerics
numpy>=1.26,<2.0

# Wire format
websockets>=13.0
msgpack>=1.1.0

# Headset viewer (HTTPS + WebSocket server for the Quest WebXR client)
aiohttp>=3.9.0

# Voice → object query (detections.track_object --voice).
# openai-whisper (not faster-whisper): faster-whisper's CTranslate2 runtime
# conflicts with the torch runtime ultralytics loads, silently aborting the
# process. openai-whisper shares torch with ultralytics.
openai-whisper>=20231117
sounddevice>=0.4.6

# Tests
pytest>=8.0

# Visualisation lives in rtabmap_viz / rviz (ROS apt packages).
# The Python side (capture.run_rtabmap) is a data subscriber for
# downstream consumers — wire protocol, agentic layer, etc.
