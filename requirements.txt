# RealSense capture (no Apple Silicon wheels; install only on the capture box)
pyrealsense2>=2.55.1; sys_platform == "linux"

# SLAM / fusion / point clouds
open3d>=0.18.0

# Object detection (pulls in torch)
ultralytics>=8.3.0

# General CV + ArUco markers
opencv-contrib-python>=4.10.0

# Numerics
numpy>=1.26,<2.0

# Wire format
websockets>=13.0
msgpack>=1.1.0

# Visualizer
rerun-sdk>=0.20.0

# Voice -> object query (capture/track_object.py --voice).
# openai-whisper rather than faster-whisper: faster-whisper's CTranslate2
# runtime conflicts with the torch runtime ultralytics loads on Mac arm64,
# silently aborting the process. openai-whisper uses the same torch.
openai-whisper>=20231117
sounddevice>=0.4.6
