[pytest]
testpaths = tests

# NOTE: if you have ROS sourced (/opt/ros/humble on the PYTHONPATH), pytest
# may auto-load incompatible ROS launch_testing plugins. Workaround:
#     PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest
# Open question whether to add a Makefile / shell wrapper. For now: env var.
